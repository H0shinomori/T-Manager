<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#26AAAAAA"
    tools:context=".Actividades.actividad_menu">

    <FrameLayout
        android:id="@+id/frameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.bottomappbar.BottomAppBar
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/bottomAppBar"
        android:layout_gravity="bottom"
        android:backgroundTint="@color/white"
        app:fabCradleMargin="10dp"
        app:fabCradleRoundedCornerRadius="50dp">
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/barraVista"
            android:layout_marginEnd="18dp"
            app:labelVisibilityMode="selected"
            app:itemIconTint="@color/color_navegacion_iconos"
            app:itemTextColor="@color/color_navegacion_iconos"
            android:background="@android:color/transparent"
            app:menu="@menu/barra_nav_menu"
            style="@style/Widget.BottomNavigationView.NoShift"
            app:elevation="0dp"/>
    </com.google.android.material.bottomappbar.BottomAppBar>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/floatingActionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/white"
        android:src="@drawable/ticket_logo"
        app:layout_anchor="@id/bottomAppBar"
        app:borderWidth="0dp"
        app:maxImageSize="30dp"
        app:tint="@color/azulito" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>


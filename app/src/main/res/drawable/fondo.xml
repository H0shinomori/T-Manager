<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Capa inferior: Color sólido -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/pastel_dos"/>
            <corners android:radius="500dp"/>
        </shape>
    </item>
    <!-- Capa superior: Imagen de fondo -->
    <item android:drawable="@drawable/textura_libreta"/>
</layer-list>
